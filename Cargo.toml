[package]
name = "milim-console"
version = "0.1.0"
edition = "2024"

[dependencies]
lua = { version = "0.1.0", path = "lua" }

[workspace]
members = ["lua"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
overflow-checks = false
incremental = false
